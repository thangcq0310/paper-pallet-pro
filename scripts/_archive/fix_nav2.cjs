const fs=require("fs");const b=fs.readFileSync("src/routes/pallet.create.tsx");const s=b.toString();const start=s.indexOf("nav({ to:");const end=s.indexOf("params: { palletId: p.palletId } });")+30;const fixed="nav({ to: "/pallet/$palletId?copies="+form.copies, params: { palletId: p.palletId } });";fs.writeFileSync("src/routes/pallet.create.tsx",s.slice(0,start)+fixed+s.slice(end));