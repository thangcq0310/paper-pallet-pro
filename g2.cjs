const fs=require("fs");const s=fs.readFileSync("src/routes/pallet.create.tsx","utf8");const pat="          </div>\r\n          <div>\r\n            <Label>Note</Label>";const idx=s.indexOf(pat);const ins="\r\n            <div>\r\n              <Label>So ban in nhan</Label>\r\n              <Input type=\x22number\x22 min={1} max={20} value={form.copies} onChange={(e) => setForm({ ...form, copies: Math.max(1, Math.min(20, +e.target.value)) })} />\r\n              <p className=\x22text-xs text-muted-foreground mt-1\x22>Toi da 20 ban</p>\r\n            </div>";fs.writeFileSync("src/routes/pallet.create.tsx",Buffer.from(s.slice(0,idx)+ins+s.slice(idx)));