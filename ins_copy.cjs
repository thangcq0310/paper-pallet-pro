const fs=require("fs");const b=fs.readFileSync("src/routes/pallet.create.tsx");const c=b.toString("utf16le");const pat="          </div>\n          <div>\n            <Label>Note</Label>";const idx=c.indexOf(pat);const ins="\n            <div>\n              <Label>So ban in nhan</Label>\n              <Input type=\"number\" min={1} max={20} value={form.copies} onChange={(e) => setForm({ ...form, copies: Math.max(1, Math.min(20, +e.target.value)) })} />\n              <p className=\"text-xs text-muted-foreground mt-1\">Toi da 20 ban</p>\n            </div>";fs.writeFileSync("src/routes/pallet.create.tsx",Buffer.from(c.slice(0,idx)+ins+c.slice(idx),"utf16le").swapCase());