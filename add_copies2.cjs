const fs=require("fs");const c=fs.readFileSync("src/routes/pallet.create.tsx");const pat=Buffer.from("<\/div>\r\n          <\/div>\r\n          <div>\r\n            <Label>Note</Label>");const idx=c.indexOf(pat);const ins=Buffer.from("<div>\r\n              <Label>So ban in nhan<\/Label>\r\n              <Input type=\"number\" min={1} max={20} value={form.copies} onChange={(e) => setForm({ ...form, copies: Math.max(1, Math.min(20, +e.target.value)) })} \/><p className=\"text-xs text-muted-foreground mt-1\">Toi da 20 ban<\/p>\r\n            <\/div>");const r=Buffer.concat([c.subarray(0,idx),ins,c.subarray(idx)]);fs.writeFileSync("src/routes/pallet.create.tsx",r);