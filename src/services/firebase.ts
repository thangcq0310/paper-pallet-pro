// Firebase is optional for this repo (localStorage mock is default).
// Keep a lightweight stub here so `tsc --noEmit` passes even when `firebase/*` deps are not installed.
export const app = null;
export const db = null;
