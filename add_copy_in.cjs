const fs=require("fs");const b=fs.readFileSync("src/routes/pallet.create.tsx");const bom=b[0]===0xef&&b[1]===0xbb&&b[2]===0xbf;const s=bom?b.subarray(3).toString("utf8"):b.toString("utf8");const pat="          </div>\n          <div>\n            <Label>Note</Label>";const idx=s.indexOf(pat);const ins="\n            <div>\n              <Label>So ban in nhan</Label>\n              <Input type=\"number\" min={1} max={20} value={form.copies} onChange={(e) => setForm({ ...form, copies: Math.max(1, Math.min(20, +e.target.value)) })} />\n              <p className=\"text-xs text-muted-foreground mt-1\">Toi da 20 ban</p>\n            </div>";fs.writeFileSync("src/routes/pallet.create.tsx",Buffer.from(s.slice(0,idx)+ins+s.slice(idx)));